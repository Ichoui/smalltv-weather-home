# Use 'openssl rand -base64 48' in a shell to create bearer tokens. Check the README.

# Généré par le commande openssl rand et donc à regénérer si besoin
HOME_ASSISTANT_WEATHER_TOKEN=dev-ha-token,

# Clef intervals.icu api
INTERVALS_API_KEY_ME=replace-with-me-api-key
INTERVALS_API_KEY_PARTNER=replace-with-partner-api-key,

# Généré par le commande openssl rand et donc à regénérer si besoin
HOME_ASSISTANT_WELLNESS_ENDPOINT_TOKEN=dev-wellness-token

# se trouve sur developer dashboard de whoop.
WHOOP_CLIENT_ID=replace-with-whoop-client-id
WHOOP_CLIENT_SECRET=replace-with-whoop-client-secret
